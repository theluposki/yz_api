import { hoursToMilliseconds } from "./hoursToMilliseconds.js";
import { tokenDateExp } from "./tokenDateExp.js";

export {
  tokenDateExp,
  hoursToMilliseconds
}

export default {
  tokenDateExp,
  hoursToMilliseconds
}
